/* Copiá este archivo a anfitrion/config.js si querés una clave fija.
   config.js no se sube a GitHub.
   passwordHash es el SHA-256 de la contraseña, en hexadecimal. Nunca escribas la contraseña. */
window.ANFITRION_CONFIG = {
  user: "anfitrion@estancialataba",
  passwordHash: "",
};
